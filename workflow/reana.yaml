version: 0.4.0
inputs:
  parameters:
  # add here optional parameter (dataset)
  # add option to load dataset in (figure out file path)
  workflow:
    type: yadage
    file: workflow.yml
  outputs:
    files:
    - results.txt
    # See if we want these graphed
