version: 0.4.0
inputs:
  files:
  - inputs/input.yml
  parameters:
  # add here optional parameter (dataset)
  workflow:
    type: yadage
    file: workflow.yml
  outputs:
    files:
    - results/output.txt
